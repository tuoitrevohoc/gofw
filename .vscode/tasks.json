{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "refresh-schema",
            "type": "shell",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "sleep 10 && cd frontend && npm run fetch-schema"
        }
    ]
} 